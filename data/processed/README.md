# Processed Example Data

This directory stores lightweight, reproducible processed artifacts derived from
the checked-in example datasets.

Generated experiment outputs that are useful for reviewer comparison should stay
small and deterministic. Larger local outputs belong under `results/`, which can
be regenerated from scripts and examples.
